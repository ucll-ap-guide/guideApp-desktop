---
name: Deploy on GitHub pages
on:
  push:
    branches:
      - story-deploy-to-github-pages
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Install 📥
        run: npm install

      - name: Build 🔧
        run: npm run build --prod

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.4
        with:
          branch: gh-pages
          folder: dist
